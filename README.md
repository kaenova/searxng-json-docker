# searxng-json-docker
container with format json enabled in settings.yml

Use docker
```sh
$ docker run -p 8080:8080 -d kaenova/searxng-json:latest
```

> This repository added a public image for searxng-json
